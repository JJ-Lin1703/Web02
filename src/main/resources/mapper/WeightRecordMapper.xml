<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
    体重记录Mapper
    用于存储用户体重记录和历史数据查询
    表名：weight_record
-->
<mapper namespace="org.example.web02.mapper.WeightRecordMapper">

    <!-- 体重记录结果映射 -->
    <resultMap id="WeightRecordResultMap" type="org.example.web02.entity.WeightRecord">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="weight" property="weight"/>
        <result column="record_date" property="recordDate"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO weight_record (user_id, weight, record_date, remark, create_time, update_time, is_deleted)
        VALUES (#{userId}, #{weight}, #{recordDate}, #{remark}, #{createTime}, #{updateTime}, 0)
    </insert>

    <select id="findByUserId" resultMap="WeightRecordResultMap">
        SELECT * FROM weight_record WHERE user_id = #{userId} AND is_deleted = 0 ORDER BY record_date DESC
    </select>

    <!-- 查询筛选后的体重历史记录（支持日期范围和排序） -->
    <select id="findByUserIdWithFilter" resultMap="WeightRecordResultMap">
        SELECT * FROM weight_record
        <where>
            user_id = #{userId} AND is_deleted = 0
            <if test="startDate != null">
                AND record_date &gt;= #{startDate}
            </if>
            <if test="endDate != null">
                AND record_date &lt;= #{endDate}
            </if>
        </where>
        <choose>
            <when test="sortBy == 'weight'">ORDER BY weight ASC</when>
            <when test="sortBy == 'weightDesc'">ORDER BY weight DESC</when>
            <when test="sortBy == 'recordDate'">ORDER BY record_date ASC</when>
            <when test="sortBy == 'recordDateDesc'">ORDER BY record_date DESC</when>
            <otherwise>ORDER BY record_date DESC</otherwise>
        </choose>
    </select>

    <select id="findByUserIdWithFilterPaginated" resultMap="WeightRecordResultMap">
        SELECT * FROM weight_record
        <where>
            user_id = #{userId} AND is_deleted = 0
            <if test="startDate != null">
                AND record_date &gt;= #{startDate}
            </if>
            <if test="endDate != null">
                AND record_date &lt;= #{endDate}
            </if>
        </where>
        <choose>
            <when test="sortBy == 'weight'">ORDER BY weight ASC</when>
            <when test="sortBy == 'weightDesc'">ORDER BY weight DESC</when>
            <when test="sortBy == 'recordDate'">ORDER BY record_date ASC</when>
            <when test="sortBy == 'recordDateDesc'">ORDER BY record_date DESC</when>
            <otherwise>ORDER BY record_date DESC</otherwise>
        </choose>
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByUserIdWithFilter" resultType="long">
        SELECT COUNT(*) FROM weight_record
        <where>
            user_id = #{userId} AND is_deleted = 0
            <if test="startDate != null">
                AND record_date &gt;= #{startDate}
            </if>
            <if test="endDate != null">
                AND record_date &lt;= #{endDate}
            </if>
        </where>
    </select>

    <select id="findRecent30DaysWeight" resultMap="WeightRecordResultMap">
        SELECT * FROM weight_record
        WHERE user_id = #{userId} 
          AND is_deleted = 0 
          AND record_date >= DATE_SUB(CURDATE(), INTERVAL 30 DAY)
        ORDER BY record_date ASC
    </select>
    <!-- 根据ID查询体重记录 -->
    <select id="findById" resultMap="WeightRecordResultMap">
        SELECT * FROM weight_record WHERE id = #{id} AND is_deleted = 0
    </select>

    <!-- 根据用户ID和日期查询体重记录 -->
    <select id="findByUserIdAndDate" resultMap="WeightRecordResultMap">
        SELECT * FROM weight_record WHERE user_id = #{userId} AND record_date = #{recordDate} AND is_deleted = 0
    </select>

    <update id="deleteById">
        UPDATE weight_record SET is_deleted = 1, update_time = NOW() WHERE id = #{id}
    </update>

    <update id="update">
        UPDATE weight_record SET weight = #{weight}, update_time = NOW() WHERE id = #{id}
    </update>

    <!-- 查询最近7天的体重值（用于体重波动检测） -->
    <select id="getWeightsInLast7Days" resultType="java.math.BigDecimal">
        SELECT weight FROM weight_record
        WHERE user_id = #{userId} 
          AND is_deleted = 0 
          AND record_date >= DATE_SUB(CURDATE(), INTERVAL 7 DAY)
        ORDER BY record_date ASC
    </select>

    <select id="checkTodayRecord" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM weight_record WHERE user_id = #{userId} AND record_date = #{recordDate} AND is_deleted = 0
    </select>
</mapper>
