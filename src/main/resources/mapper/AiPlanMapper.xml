<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.web02.mapper.AiPlanMapper">

    <resultMap id="AiPlanResultMap" type="org.example.web02.entity.AiPlan">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="plan_title" property="planTitle"/>
        <result column="total_calorie" property="totalCalorie"/>
        <result column="plan_content" property="planContent"/>
        <result column="version_no" property="versionNo"/>
        <result column="parent_plan_id" property="parentPlanId"/>
        <result column="generate_time" property="generateTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_plan (user_id, plan_title, total_calorie, plan_content, version_no, parent_plan_id, generate_time, update_time, is_deleted)
        VALUES (#{userId}, #{planTitle}, #{totalCalorie}, #{planContent}, #{versionNo}, #{parentPlanId}, #{generateTime}, #{updateTime}, 0)
    </insert>

    <!-- 根据用户ID查询所有计划（按生成时间倒序） -->
    <select id="findByUserId" resultMap="AiPlanResultMap">
        SELECT * FROM ai_plan WHERE user_id = #{userId} AND is_deleted = 0 ORDER BY generate_time DESC
    </select>

    <!-- 分页查询用户计划 -->
    <select id="findByUserIdPaginated" resultMap="AiPlanResultMap">
        SELECT * FROM ai_plan WHERE user_id = #{userId} AND is_deleted = 0 ORDER BY generate_time DESC LIMIT #{offset}, #{limit}
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(*) FROM ai_plan WHERE user_id = #{userId} AND is_deleted = 0
    </select>

    <select id="findById" resultMap="AiPlanResultMap">
        SELECT * FROM ai_plan WHERE id = #{id} AND is_deleted = 0
    </select>

    <update id="deleteById">
        UPDATE ai_plan SET is_deleted = 1 WHERE id = #{id}
    </update>

    <!-- 更新计划内容 -->
    <update id="updateById">
        UPDATE ai_plan 
        SET plan_title = #{planTitle}, 
            total_calorie = #{totalCalorie}, 
            plan_content = #{planContent}, 
            version_no = #{versionNo}, 
            parent_plan_id = #{parentPlanId}, 
            generate_time = #{generateTime}, 
            update_time = #{updateTime}, 
            is_deleted = #{isDeleted}
        WHERE id = #{id}
    </update>

    <select id="findLatestByUserId" resultMap="AiPlanResultMap">
        SELECT * FROM ai_plan WHERE user_id = #{userId} AND is_deleted = 0 ORDER BY generate_time DESC LIMIT 1
    </select>
</mapper>
