server:
  port: 8080

spring:
  application:
    name: smart-health-helper
  datasource:
    url: jdbc:mysql://localhost:3306/smart_health_helper?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: ljj20051101
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 300000
      connection-timeout: 20000
      max-lifetime: 1200000

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: org.example.web02.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: smart-health-helper-jwt-secret-key-2024
  expiration: 604800000

logging:
  level:
    org.example.web02: DEBUG
    org.mybatis: DEBUG
